let num = 10;


if (num > 10) {
  res = "Maior que 10";
  document.write(res);
  alert(res);
} else {
  res = "Menor que 10";
  document.write(res);
  alert(res);
}


// Blocos de comando { }

// Delimita o início e o fim do bloco de comando {   }

// O uso das chaves { } é obrigatório quando eu quiser delimitar um número maior do que um de instruções de um bloco.
